// TODO(backend-eng): flat movie scanner.
// Files directly under root, OR single-video folders (folder name = title).
// group_id = NULL. Strip release tags `[1080p] (2023) .WEB-DL .x264` from titles.
// See plan, Agent A.
