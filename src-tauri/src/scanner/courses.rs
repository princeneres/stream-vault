// TODO(backend-eng): 2-level course scanner.
// Top-level folder = course (group). Sub-folders = modules (groups w/ parent).
// Files inside modules = items. No sub-folder => implicit "Main" module.
// Parse leading numeric prefixes for position + title. See plan, Agent A.
