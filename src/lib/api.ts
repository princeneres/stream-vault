// TODO(frontend-eng): typed wrappers around Tauri `invoke` + `listen`.
// One fn per command in src-tauri/src/commands.rs. Mirror snake_case command
// names but expose camelCase fn names. See plan, Agent B.

export {};
